using System;

class MainClass {
  public static void Main (string[] args) {
Console.WriteLine("Skriv in ditt personnummer ");
string personnummer = Console.ReadLine();
double personNummer = double.Parse(personnummer);
double gräns_För_personnummer = 19101010000;
if (personNummer != gräns_För_personnummer)
if (personNummer > gräns_För_personnummer)
Console.WriteLine("18" + personNummer);
else 
Console.WriteLine("19" + personNummer);
double ny_gräns_personnummer = 1210100000;
if (ny_gräns_personnummer < personNummer)
Console.WriteLine("20" + personnummer);
  }
}
